version: '3.3'
services:
  db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_DATABASE: 'capcoast'
      # So you don't have to use root, but you can if you like
      MYSQL_USER: 'user'
      # You can use whatever password you like
      MYSQL_PASSWORD: 'password'
      # Password for root access
      MYSQL_ROOT_PASSWORD: 'password'
    volumes:
      - type: volume
      - source: db-volume
      - target: /data/db

  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    enviroments:
      - DATABASE_NAME=capcoast
      - DATABASE_USER=user
      - DATABASE_PASSWORD=password
      - DATABASE_HOST=localhost
      - DATABASE_PORT=3306
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    links:
    - db




# Names our volume
volumes:
  my-db: